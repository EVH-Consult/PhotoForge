# Metadata Extractors Package — INTERNAL

This package initializer exports the public extractor functions for EXIF,
filename, filesystem, folder, HEIC, JPEG, PNG, RAW, and video sources. It
contains no extraction, precedence, resolution, or normalization logic.
`__all__` is the deterministic public export list.
